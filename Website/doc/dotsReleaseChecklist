Preparations
  - copy to allgenes site:
     - blastable files
     - downloadSite files
  - servlet-config:
      - point to new blastable files
      - write news announcing the new release
  - index.html:
      - update manually curated stats
      - update count of aligned transcripts
      - mention new release
      - update Citing Allgenes section
  - credit.html:
      - update
  - InstallConfig:
      - update external db rel ids
  - update blatTargetDBP.Labels in dotsgenes-config if new genome release
  - update GUS/perl/servlet/dotsgenes/blatLink.pl.in if new genome release
  - remind EpCon to deal with the new release

Final
  - copy statistics page to allgenes site
  - remove all cached values on allgenes site
  - redirect download site current/ to point to new release
